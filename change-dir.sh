#!/bin/bash

cd /home
pwd
ls

#Do source change-dir.sh to go in that dir
#Dont Use exit code when using source because if the script is sourced within the current shell, then the current shell is closed

